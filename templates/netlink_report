<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Netlink Report</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }

        /* ✅ TITLE */
        h2 {
            text-align: center;
            font-weight: bold;
            margin-bottom: 25px;
        }

        /* ✅ TABLE */
        table {
            width: 80%;
            margin: auto;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
        }

        /* ✅ HEADINGS BOLD */
        th {
            font-weight: bold;
        }
    </style>
</head>

<body>

    <!-- ✅ BOLD TITLE -->
    <h2>Netlink Report</h2>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Person</th>
                <th>Credit</th>
                <th>Debit</th>
                <th>Balance</th>
            </tr>
        </thead>

        <tbody>
        {% for e in entries %}
            <tr>
                <td>{{ e.date }}</td>
                <td>{{ e.person }}</td>
                <td>{% if e.credit != "-" %}₹ {{ e.credit }}{% else %}-{% endif %}</td>
                <td>{% if e.debit != "-" %}₹ {{ e.debit }}{% else %}-{% endif %}</td>
                <td>₹ {{ e.balance }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</body>
</html>
